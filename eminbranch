#!groovy

        stage('PARAMETRES'){
          
                properties([
  parameters([
    string(name: 'submodule', defaultValue: ''),
    string(name: 'submodule_branch', defaultValue: ''),
    string(name: 'commit_sha', defaultValue: ''),
  ])
])
          
   }
stage('ECHO'){
echo "deneme jobı scann   sonnn"
           echo "INFO: BRANCH NAME: ${params.submodule}"
        }
