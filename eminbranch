#!groovy

        stage('Last Five Commits'){
          echo "deneme jobı scann   sonnn"
                properties([
  parameters([
    string(branch: 'submodule', defaultValue: ''),
    string(name: 'submodule_branch', defaultValue: ''),
    string(name: 'commit_sha', defaultValue: ''),
  ])
])
           echo "INFO: BRANCH NAME: $branch"
        }
